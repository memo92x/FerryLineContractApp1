@model FerryLineContractApp.Models.Dock

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Dock</legend>

        @Html.HiddenFor(model => model.DockId)

        <div class="editor-label">
            @Html.LabelFor(model => model.DockName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DockName)
            @Html.ValidationMessageFor(model => model.DockName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.FerryCapacity)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FerryCapacity)
            @Html.ValidationMessageFor(model => model.FerryCapacity)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
